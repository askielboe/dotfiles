return {
  "gbprod/substitute.nvim",
  enabled = false,
  -- opts = {
  --   -- your configuration comes here
  --   -- or leave it empty to use the default settings
  --   -- refer to the configuration section below
  -- },
  keys = {
    { "n", "s", ":substitude.operator<CR>", { noremap = true } },
    -- { "n", "ss", substitude.line, { noremap = true } },
    -- { "n", "S", substitude.eol, { noremap = true } },
    -- { "x", "s", substitude.visual, { noremap = true } },
  },
}
